// Copy this file to local.xcconfig and set your Apple Development Team ID.
// local.xcconfig is gitignored — it holds per-developer signing settings.
//
//   cp local.xcconfig.example local.xcconfig
//
DEVELOPMENT_TEAM = YOUR_TEAM_ID

// Backend URL for device testing (set to your Mac's local IP + port).
// Leave empty to use mock mode. Note: $() is needed to avoid // being
// treated as an xcconfig comment.
// BACKEND_URL = http:/$()/YOUR_IP:8001
